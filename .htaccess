RewriteEngine On

# Se arquivo ou diretorio existe, serve direto
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Tudo que nao e arquivo real vai para o front controller
RewriteRule ^ index.php [QSA,L]
